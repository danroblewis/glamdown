/**
 * List block renderer — bullet, ordered, and task lists.
 */
import type { ListData } from 'blecsd/utils';
import type { Theme } from '../theme.js';
export declare function renderList(data: ListData, theme: Theme, _width: number): string[];
