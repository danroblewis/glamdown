/**
 * Table block renderer — Unicode box-drawing table.
 */
import type { TableData } from 'blecsd/utils';
import type { Theme } from '../theme.js';
export declare function renderTable(data: TableData, theme: Theme, _width: number): string[];
